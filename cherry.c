#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
    char *b = "worry";
    char *d = malloc(strlen(b) + 1);
}
    for (int i = 0; i < strlen(b); i++)
       {
         d[i] = b[i];
        {
            d[0] = toupper(b[0]);

            printf("b: %s\n", b);
            printf("d: %s\n", d);
            return 0;
        {

        if(strlen(b) > 0)

            d[worry] = toupper(d[worry])
        }
          printf("b: %s\n", b);
          printf("d: %s\n", d);
          return 0;
          {
            if (strlen(d) > 0)
          }
          printf("wrong\n");
    }
    return 1;
}
